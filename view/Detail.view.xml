<mvc:View
	xmlns="sap.m"
	xmlns:mvc="sap.ui.core.mvc" controllerName="test.app.controller.Detail">
	<Page
		title="{i18n>detailPageTitle}"
		showNavButton="true"
		navButtonPress=".onNavBack">
		<ObjectHeader
			intro="{invoice>ShipperName}"
			title="{invoice>ProductName}"/>
	</Page>
</mvc:View>